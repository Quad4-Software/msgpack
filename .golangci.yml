version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
  settings:
    staticcheck:
      checks:
        - all
        - "-SA1019"
        - "-QF1001"
        - "-ST1000"
        - "-ST1003"
        - "-ST1016"
        - "-ST1020"
        - "-ST1021"
        - "-ST1022"
        - "-ST1023"
  exclusions:
    rules:
      - path: _test\.go$
        linters:
          - unused
      - path: example_.*_test\.go$
        linters:
          - unused
      - text: "Constant reflect\\.(Ptr|Struct|Slice|Map|Interface|String|Bool|Int|Uint|Float) should be inlined"
        linters:
          - govet
      - text: "Call of reflect\\.PtrTo should be inlined"
        linters:
          - govet
      - text: "func \\(\\*Encoder\\)\\.encodeStringSlice is unused"
        linters:
          - unused
      - text: "func nilableType is unused"
        linters:
          - unused

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
